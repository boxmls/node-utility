The module has S3 utility to manage the `cdn.boxmls.com` bucket on AWS S3.

## Usage

```js
var utility = require('boxmls/utility');

var s3 = utility.getS3( {
  bucket: "{bucket}"
} );

s3.uploadFromBuffer( filePath, Buffer, options, callback );
```